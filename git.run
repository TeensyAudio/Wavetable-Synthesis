cd Wavetable-Synthesis
git checkout master-backup
git add *
curtime=`date`
git commit -m "Automatic Backup @ $curtime"
git push origin master-backup
git checkout soundfont_backup
git add *
curtime=`date`
git commit -m "Automatic Backup @ $curtime"
git push
git checkout teensy-backup
git add *
curtime=`date`
git commit -m "Automatic Backup @ $curtime"
git push
